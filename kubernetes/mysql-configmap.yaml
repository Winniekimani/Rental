apiVersion: v1
kind: ConfigMap
metadata:
  name: mysql-config-map
data:
  MYSQL_HOST: mysql# The name of mysql service
  MYSQL_DATABASE: Rentals   # The name of the database used  app
  MYSQL_USER: root     #  user for  app